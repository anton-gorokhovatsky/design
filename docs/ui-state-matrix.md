# Матрица состояний интерфейса

Этот документ — небольшой статический workbench для vanilla HTML/CSS/JS-сайта.
Он фиксирует состояния, которые иначе остаются скрытыми в условных ветках
`script.js` и поздних слоях `styles.css`.

Storybook для проекта не нужен: компонентов немного, сборщика нет, а источник
истины — реальная полноэкранная композиция. Нужны воспроизводимые состояния и
рендеры, а не новый runtime.

## Принцип проверки

1. Сначала запустить `node scripts/audit-project.mjs`.
2. Выбрать из таблицы состояния, которых касается изменение.
3. Снять рендеры до и после в одинаковых viewport, theme и UI-state.
4. Проверить computed styles спорных элементов, а не делать вывод только по
   позиции декларации в `styles.css`.
5. Для финального прохода выполнить всю матрицу.

Имена артефактов: `<viewport>-<theme>-<state>.png`, например
`1440x900-dark-project-reel.png`.

## Базовые viewport

| ID | Размер | Назначение |
|---|---:|---|
| `desktop` | `1440×900` | Полная пространственная композиция и drag-модули |
| `tablet` | `1024×768` | Промежуточная компоновка и ограниченная ширина панелей |
| `mobile` | `390×844` | Основной touch-сценарий |
| `reflow` | `320×720` | Минимальная ширина и эквивалент zoom/reflow-проверки |

Каждый базовый viewport проверяется в light и dark. Для релевантных состояний
добавляются keyboard focus, reduced motion и forced colors/high contrast.

## Каталог состояний

| State | Как получить | Что проверять |
|---|---|---|
| `map-idle` | Загрузить страницу, не открывать inspector | Иерархия карты, читаемость осей, «Гараж» как крупнейшая точка, отсутствие ложных поверхностей |
| `garage-selected` | Активировать точку «Музей „Гараж“» | Положение фрагментов readout, отсутствие перекрытия консолей, ссылка и close, возвращение focus |
| `running-selected` | Активировать точку `БЕГ ᕕ( ᐛ )ᕗ` | Второй центр притяжения остаётся в личной зоне, меньше «Гаража», ссылка на highlight и readout доступны без overflow |
| `project-selected` | Активировать любой независимый проект | Тот же interaction contract при другой длине title/meta/description |
| `project-reel` | На desktop навести указатель или сфокусировать проект с `previewVideo` | Видео `contain`, 4:5, без chrome и рамки, центральная композиция остаётся читаемой |
| `filter-company` | `VIEW → КОМПАНИИ` | Предсказуемое приглушение остальных типов без смещения узлов |
| `filter-project` | `VIEW → ПРОЕКТЫ` | То же поведение и сохранение выбранной точки |
| `filter-personal` | `VIEW → ЛИЧНОЕ` | То же поведение, достаточный контраст небольших точек |
| `filter-practice` | `VIEW → ПРИНЦИПЫ` | Все 17 принципов доступны как отдельные точки |
| `search-default` | Сфокусировать пустое поле поиска | Видимый focus, корректная связь input/listbox, результаты не перекрывают управление |
| `search-match` | Ввести `гараж` или название проекта | Совпадения, keyboard route, отсутствие ложных результатов |
| `search-empty` | Ввести строку без совпадений | Понятное empty-state сообщение без декоративной заглушки |
| `panel-work` | Открыть `ПРОЕКТЫ` | Плавающие фрагменты вместо drawer, строки проектов, close и focus trap/return |
| `panel-approach` | Открыть `ПОДХОД` | Длинный текст, непрерывная типографика, скролл и reflow |
| `panel-contact` | Открыть `КОНТАКТ` | Email не выходит за контейнер при zoom и 320 px |
| `mobile-nav-closed` | Загрузить при ширине ≤680 px | Компактная кнопка не перекрывает карту и имеет понятное имя |
| `mobile-nav-open` | Открыть мобильную орбиту | Все маршруты видимы, доступны и не выходят за viewport |
| `console-dragged` | На desktop перетащить каждый из трёх модулей за свободный материал | Controls не перехватываются drag, модуль остаётся в viewport, resize корректно ограничивает offset |
| `keyboard-focus` | Пройти страницу только Tab/Shift+Tab/Enter/Space/Arrows/Escape | Порядок, видимость focus, отсутствие ловушек, возврат к trigger |
| `reduced-motion` | Эмулировать `prefers-reduced-motion: reduce` | Нет морфинга/путешествия, конечные состояния и управление сохранены |
| `forced-colors` | Включить forced colors/high contrast | Точки, controls, focus и selected state различимы без reliance на tint/blur |
| `text-zoom` | 200% zoom и ширина 320 CSS px | Текст не обрезан, обычный контент не требует горизонтального скролла |

## Accessibility tree

Для контрольного прохода проверить:

- обе skip-ссылки;
- доступное имя и keyboard help у canvas;
- группу точек карты и уникальное имя каждой кнопки;
- `aria-pressed` у фильтров и выбранной точки;
- `aria-expanded` и `aria-controls` у поиска и mobile navigation;
- `aria-hidden` + `inert` у закрытых inspector/content panel;
- возврат focus после закрытия inspector и контентной группы;
- отсутствие декоративных specks и ASCII в accessibility tree;
- отсутствие существенного контента только в hover/video.

## Семантика визуализации

Каждый проход по карте отвечает на пять вопросов:

1. Какую реальную сущность или отношение представляет элемент?
2. Что является одной единицей данных?
3. Какой визуальный атом представляет эту единицу?
4. Какие свойства данных управляют `x`, `y`, размером, формой, цветом и линией?
5. Как фильтры, поиск и readout показывают тот же набор данных с другой точки
   зрения?

Если на вопрос нет ответа, элемент либо декоративный и должен выглядеть
вторично, либо является кандидатом на удаление.

## Условия готовности

- Нет новых необъяснённых material, type, corner или motion variants.
- В затронутых состояниях нет clipping, overlap и горизонтального overflow.
- Результат одинаково понятен с мышью, клавиатурой и touch.
- Системные theme, font-size/zoom, reduced-motion и contrast preferences не
  ломают доступ к информации.
- Финальный вывод основан на реальных рендерах, а не только на diff и тестах.

## Контрольный проход 2026-07-24

Фактически отрендерены desktop `1440×900`, tablet `1024×768`, mobile
`390×844` и reflow `320×720` в light/dark. Дополнительно проверены
`garage-selected`, `running-selected`, `project-selected`, `project-reel`,
четыре фильтра, поиск, три контентные группы, мобильная навигация и drag всех
трёх desktop-консолей.

- На `320×720` document/body остаются шириной 320 px; видимые потомки
  `panel-contact` не пересекают границы viewport.
- У canvas подтверждены keyboard focus и изменение реального рендера после
  `Home` → `Shift+ArrowRight`; drag завершается без зависшего `is-dragging`.
- Keyboard route проверен для roving map, поиска, inspector, mobile navigation
  и dialog focus trap/return.
- Контраст обычного текста: минимум 4,95:1 в light и 9,50:1 в dark.
  Контраст focus-сигнала: минимум 5,76:1 в light и 4,32:1 в dark.
- `prefers-reduced-motion: reduce` и `forced-colors: active` дополнительно
  отрендерены в headless Chromium на `390×844`. В reduced motion canvas
  остаётся неподвижным без ввода в течение 600 мс, реагирует на клавиатуру, а
  видео остаются paused. В forced colors видимы все 46 узлов и отдельный
  focus-сигнал точки бега; ошибок консоли нет.
- `node scripts/check-project.mjs`: PASS, 0 errors, 0 warnings; 46 точек карты,
  9 связей, 17 принципов и 13 шоурилов.
